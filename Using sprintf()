#include <stdio.h>
#include <string.h>

int main() {
    char s1[50] = "Hello "; 
    char s2[] = "Geeks";

    // Concatenate s2 to s1 using sprintf
    sprintf(s1 + strlen(s1), "%s", s2);

    printf("%s", s1);
    return 0;
}
